# 数据目录

本目录用于放置比赛数据和中间处理结果。当前打包版本未包含原始数据。

建议目录结构：

```text
data/
├── raw/          # 原始车辆数据
├── processed/    # 清洗后的中间数据
└── results/      # 模型预测结果和图像
```

请根据脚本中的输入输出路径，把数据文件放到对应位置，或修改脚本中的路径变量。
